import * as RNComponents from 'react-native';
import DrawerContent from './DrawerContent';

const RN = {
    View: RNComponents.View,
    Text: RNComponents.Text,
    SafeAreaView: RNComponents.SafeAreaView,
    TextInput: RNComponents.TextInput,
    Image: RNComponents.Image,
    TouchableOpacity: RNComponents.TouchableOpacity,
    ActivityIndicator: RNComponents.ActivityIndicator,
}

const AVANTICA = {
    DrawerContent,
}

export { RN, AVANTICA };